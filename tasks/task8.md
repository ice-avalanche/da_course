# Домашнее задание №8

## Агрегация и сегментация данных

**Цель:** научиться агрегировать данные, строить сводные таблицы и сегментировать объекты для анализа.

## 1. Создание ноутбука

Перейдите по ссылке:
[https://colab.new](https://colab.new)

Создайте новый файл.

## 2. Подготовка среды

Вставьте код в первую ячейку и запустите:

```python
import pandas as pd
import numpy as np
```

## 3. Исходные данные

Создайте DataFrame:

```python
df = pd.DataFrame({
    "client_id": [1,1,2,2,3,3,4,5,5,6],
    "region": ["Москва","Москва","СПб","СПб","Москва","Казань","Казань","Москва","СПб","СПб"],
    "product": ["A","B","A","C","B","A","C","A","B","C"],
    "date": pd.to_datetime([
        "2024-01-01","2024-02-01",
        "2024-01-10","2024-03-01",
        "2024-02-15","2024-02-20",
        "2024-03-05","2024-01-07",
        "2024-02-17","2024-03-10"
    ]),
    "revenue": [100,200,150,300,250,180,220,130,170,260]
})

df
```

## 4. Агрегация данных

Рассчитайте по каждому клиенту:
* суммарную выручку;
* количество покупок;
* средний чек.

Подсказка:

```python
df.groupby("...").agg(
    ...=("...", "..."),
    ...=("...", "..."),
    ...=("...", "...")
)
```

## 5. Многоуровневая агрегация

Постройте выручку:
* по регионам;
* по регионам и товарам.

Подсказка:

```python
df.groupby(["...", "..."])["..."].sum()
```

## 6. Pivot-таблица

Постройте таблицу:
* строки – регионы;
* колонки – товары;
* значения – выручка.

Подсказка:

```python
pd.pivot_table(
    df,
    index="...",
    columns="...",
    values="...",
    aggfunc="...",
    fill_value=0
)
```

## 7. Сегментация клиентов

1. Постройте агрегированную таблицу по клиентам.
2. Разбейте клиентов на 3 группы по выручке.

Подсказка:

```python
agg["segment"] = pd.qcut(
    agg["..."],
    q=3,
    labels=["low","medium","high"]
)
```

## 8. ABC-анализ

Выполните:

* сортировку по выручке;
* долю в общей выручке;
* накопленную долю;
* присвоение A/B/C.

Подсказка:

```python
df = df.sort_values(by="...", ascending=False)
df["share"] = df["..."] / df["..."].sum()
df["cum_share"] = df["..."].cumsum()
```

## 9. Биннинг

Разбейте клиентов по выручке на интервалы.

Подсказка:

```python
pd.cut(
    agg["..."],
    bins=[...],
    labels=[...]
)
```

## Формат сдачи

* ссылка на Google Colab (нажать в открытом файле Colab «Поделиться», открыть доступ всем, скопировать ссылку);
* код выполняется сверху вниз без ошибок.
