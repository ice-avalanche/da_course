# Домашнее задание №8

## Визуализация данных в Python

**Цель:** научиться строить и настраивать графики для анализа данных.

## 1. Создание ноутбука

Перейдите по ссылке:
[https://colab.new](https://colab.new)

Создайте новый файл.

## 2. Подготовка среды

Вставьте код в первую ячейку и запустите:

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
```

## 3. Исходные данные

Создайте DataFrame:

```python
df = pd.DataFrame({
    "дата": pd.date_range(start="2024-01-01", periods=12, freq="M"),
    "продажи": [100, 120, 130, 90, 150, 170, 160, 180, 200, 210, 190, 220],
    "категория": ["A", "B", "A", "B", "A", "B", "A", "B", "A", "B", "A", "B"]
})

df
```

## 4. Линейный график

Постройте динамику продаж по времени.

Подсказка:

```python
plt.plot(df["..."], df["..."])
plt.show()
```

## 5. Столбчатая диаграмма

Постройте график продаж по категориям.

Подсказка:

```python
df.groupby("...")["..."].sum().plot(kind="bar")
plt.show()
```

## 6. Гистограмма

Постройте распределение продаж.

Подсказка:

```python
plt.hist(df["..."])
plt.show()
```

## 7. Boxplot

Определите наличие выбросов.

Подсказка:

```python
sns.boxplot(x=df["..."])
```

## 8. Настройка графиков

Добавьте:

* заголовок;
* подписи осей;
* сетку.

Подсказка:

```python
plt.title("...")
plt.xlabel("...")
plt.ylabel("...")
plt.grid()
```

## 9. Визуализация категорий

Постройте сравнение категорий.

Подсказка:

```python
sns.barplot(x="...", y="...", data=df)
```

## 10. Использование ИИ для улучшения визуализации

В Google Colab воспользуйтесь встроенным ИИ, чтобы улучшить внешний вид графиков, добавить стилизацию и сделать визуализации более наглядными.
Чтобы открыть чат с Gemini, нажмите на кнопку с логотипом Gemini посередине внизу веб-страницы).

<img src="https://upload.wikimedia.org/wikipedia/commons/thumb/8/8f/Google-gemini-icon.svg/3840px-Google-gemini-icon.svg.png" width="32">

Можете сформулировать любой запрос на свой вкус, или адаптировать к своей задаче примеры ниже:

> 1. Перепиши этот код графика, используя seaborn вместо matplotlib. Добавь стиль whitegrid, увеличь размер графика и сделай цвета более контрастными.

> 2. Добавь к этому графику заголовок, подписи осей, сетку, легенду и увеличь шрифт всех подписей.

> 3. Сделай этот линейный график более информативным, добавь маркеры точек в виде квадратов, подпиши значения на последних 3 точках и выдели максимальное значение.

> 4. Измени этот bar chart, отсортируй категории по убыванию, добавь подписи значений над столбцами и сделай горизонтальный график.

> 5. Улучши гистограмму, добавь KDE (плотность), увеличь количество бинов до 10-15 и подпиши оси.

> 6. Для boxplot: добавь подпись медианы, измени цвет коробки и сделай график более читаемым (размер, стиль).
 
> 7. В графике с временным рядом поверни подписи дат на 45 градусов, добавь сетку и увеличь отступы, чтобы подписи не накладывались.

> 8. Перепиши код так, чтобы использовалась цветовая палитра (palette) и разные категории отображались разными цветами.

> 9. Сделай график в стиле отчета, минималистичный дизайн, без лишних линий, акцент на данных.
 
> 10. Добавь аннотации, подпиши максимальное и минимальное значение на графике с указанием координат.

## Формат сдачи

* ссылка на Google Colab (нажать в открытом файле Colab «Поделиться», открыть доступ всем, скопировать ссылку);
* не менее 5 графиков;
* графики оформлены (заголовки, оси);
* ноутбук выполняется без ошибок.
