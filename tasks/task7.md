# Домашнее задание №7

## Очистка и подготовка данных

**Цель:** научиться приводить «грязные» данные в корректный вид и готовить датасет для моделирования.

## 1. Создание ноутбука

Перейдите по ссылке:
[https://colab.new](https://colab.new)

Создайте новый файл.

## 2. Подготовка среды

Вставьте код в первую ячейку и запустите:

```python
import pandas as pd
import numpy as np
```

## 3. Исходные данные

Создайте DataFrame:

```python
df = pd.DataFrame({
    "region": ["Москва", "СПб", "Москва", None, "Казань", "СПб"],
    "revenue": ["100", "200", "150", "120", "1000", "200"],
    "cost": [70, 120, None, 80, 50, 120]
})

df
```

## 4. Поиск проблем в данных

Определите:

* есть ли пропуски;
* типы данных;
* есть ли дубликаты;
* есть ли подозрительные значения.

Подсказка:

```python
df.info()
df.isna().sum()
df.duplicated()
```

## 5. Обработка пропусков

Удалите строки с пропусками.

Подсказка:

```python
df = df.dropna()
```

## 6. Преобразование типов

Приведите числовые данные к корректному типу.

Подсказка:

```python
df["..."] = pd.to_numeric(df["..."])
```

## 7. Работа с дубликатами

Проверьте наличие дубликатов и удалите их.

Подсказка:

```python
df = df.drop_duplicates()
```

## 8. Обработка выбросов

Удалите значения с аномально высокой выручкой (>500).

Подсказка:

```python
df = df[df["..."] <= ...]
```

## 9. Создание признаков

Добавьте:

* прибыль;
* маржинальность.

Подсказка:

```python
df["..."] = df["..."] - df["..."]
df["..."] = df["..."] / df["..."]
```

## Формат сдачи

* ссылка на Google Colab (нажать в открытом файле Colab «Поделиться», открыть доступ всем, скопировать ссылку);
* ноутбук должен быть выполнен сверху вниз без ошибок.
