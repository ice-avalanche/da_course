# Домашнее задание №6

## Введение в Python и первичный анализ данных (Google Colab)

**Цель:** закрепить навыки работы с Python и pandas: создание таблиц, вычисления, фильтрация и первичный анализ данных.

## 1. Создание ноутбука

Перейдите по ссылке:
[https://colab.new](https://colab.new)

Создайте новый файл.

В Colab работа ведется через ячейки (cells):
* код вводится в ячейку;
* запуск осуществляется нажатием кнопки ▶ или Shift + Enter;
* результат появляется под ячейкой.

## 2. Подготовка среды

Вставьте код в первую ячейку и запустите ее:

```python id="wjbo01"
import pandas as pd
import numpy as np
```

## 3. Исходные данные

Создайте новую ячейку, вставьте код и запустите:

```python id="dust82"
df = pd.DataFrame({
    "регион": ["Москва", "СПб", "Казань", "Москва", "СПб", "Казань"],
    "выручка": [120, 200, 150, 180, 220, 130],
    "затраты": [80, 120, 90, 100, 140, 85]
})

df
```

## 4. Расчет показателей

Добавьте:

* прибыль
* маржинальность

Подсказка:

```python id="n1wyyd"
df["..."] = df["..."] - df["..."]
df["..."] = df["..."] / df["..."]
```

## 5. Первичный анализ (EDA)

Выполните:

* просмотр данных
* структура
* статистика

Подсказка:

```python id="6yknf6"
df.head()
df.info()
df.describe()
```

## 6. Работа со столбцами

Выведите:

* один столбец
* несколько столбцов

Подсказка:

```python id="d5qn57"
df["..."]
df[["...", "..."]]
```

## 7. Фильтрация

Найдите строки:

* одно условие
* несколько условий

Подсказка:

```python id="y4h4qp"
df[df["..."] > ...]
df[(df["..."] > ...) & (df["..."] < ...)]
```

## 8. Анализ категорий

Определите:

* уникальные значения
* количество

Подсказка:

```python id="sh7qdt"
df["..."].unique()
df["..."].value_counts()
```

## 9. Агрегации

Рассчитайте показатели

Подсказка:

```python id="4cuuuk"
df["..."].mean()
df["..."].max()
df["..."].min()
```

## Формат сдачи

* ссылка на Google Colab (нажать в открытом файле Colab «Поделиться», открыть доступ всем, скопировать ссылку);
* ноутбук должен быть выполнен сверху вниз без ошибок.
